[package]
name = "clear-filesystem-interface"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.44"
iced = { version = "0.14", features = ["svg", "image", "tokio"] }
tokio = { version = "1", features = ["full"] }
